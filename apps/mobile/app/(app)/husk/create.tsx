import { useState } from "react"; import { router } from "expo-router"; import { View, StyleSheet } from "react-native"; import { AppText, Button } from "../../../src/components"; import { ErrorState, LoadingState } from "../../../src/components/States"; import { ReminderForm } from "../../../src/features/husk/components/ReminderForm"; import { defaultReminderForm, type ReminderForm as Form } from "../../../src/features/husk/reminderForm"; import { useCreateReminder } from "../../../src/features/husk/hooks/useReminderMutations"; import { useCalendarFamilyMembers } from "../../../src/features/calendar/hooks/useCalendarFamilyMembers"; import { theme } from "../../../src/theme/tokens";
export default function CreateReminderScreen(){const mutation=useCreateReminder();const members=useCalendarFamilyMembers();const [form,setForm]=useState<Form>(()=>defaultReminderForm());const update=<K extends keyof Form>(k:K,v:Form[K])=>{mutation.resetError();setForm(x=>({...x,[k]:v}));};if(mutation.familiesLoading)return <LoadingState title="Klargjør Husk" description="Henter familien før påminnelsen lagres."/>;if(mutation.missingContext)return <ErrorState title="Mangler familietilgang" description="Vi finner ikke en aktiv familie." onRetry={()=>router.replace("/(app)/(tabs)/tasks")}/>;return <View style={styles.root}><View style={styles.top}><Button title="Tilbake" variant="secondary" onPress={()=>router.back()}/><AppText variant="label">Husk</AppText></View><ReminderForm title="Ny påminnelse" description="Opprett en påminnelse for familien." form={form} onChange={update} onSubmit={()=>mutation.create(form)} onCancel={()=>router.back()} submitting={mutation.saving} error={mutation.error} familyMembers={members.familyMembers} submitTitle="Lagre påminnelse"/></View>}; const styles=StyleSheet.create({root:{flex:1,backgroundColor:theme.colors.background},top:{padding:theme.spacing.lg,flexDirection:"row",justifyContent:"space-between",alignItems:"center"}});
