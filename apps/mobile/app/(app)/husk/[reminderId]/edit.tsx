import { useEffect, useState } from "react"; import { router, useLocalSearchParams } from "expo-router"; import { View, StyleSheet } from "react-native"; import { AppText, Button } from "../../../../src/components"; import { EmptyState, ErrorState, LoadingState } from "../../../../src/components/States"; import { ReminderForm } from "../../../../src/features/husk/components/ReminderForm"; import { reminderToForm, type ReminderForm as Form } from "../../../../src/features/husk/reminderForm"; import { useReminderDetails } from "../../../../src/features/husk/hooks/useReminderDetails"; import { useUpdateReminder } from "../../../../src/features/husk/hooks/useReminderMutations"; import { useCalendarFamilyMembers } from "../../../../src/features/calendar/hooks/useCalendarFamilyMembers"; import { theme } from "../../../../src/theme/tokens";
export default function EditReminder(){const {reminderId}=useLocalSearchParams<{reminderId?:string}>();const id=typeof reminderId==="string"?reminderId:null;const details=useReminderDetails(id);const mutation=useUpdateReminder(id??"");const members=useCalendarFamilyMembers();const [form,setForm]=useState<Form|null>(null);useEffect(()=>{if(details.reminder&&!mutation.saving)setForm(reminderToForm(details.reminder));},[details.reminder,mutation.saving]);if(!id)return <ErrorState title="Ugyldig påminnelse" description="Mangler id." onRetry={()=>router.replace("/(app)/(tabs)/tasks")}/>;if(details.loading||mutation.familiesLoading||!form)return <LoadingState title="Laster påminnelse" description="Henter påminnelsen før den redigeres."/>;if(details.error)return <ErrorState title="Kunne ikke hente påminnelse" description="Prøv igjen." onRetry={()=>void details.refetch()}/>;if(!details.reminder)return <EmptyState title="Påminnelsen finnes ikke" description="Den kan være slettet."/>;const update=<K extends keyof Form>(k:K,v:Form[K])=>{mutation.resetError();setForm(x=>x?{...x,[k]:v}:x);};return <View style={styles.root}><View style={styles.top}><Button title="Tilbake" variant="secondary" onPress={()=>router.back()}/><AppText variant="label">Husk</AppText></View><ReminderForm title="Rediger påminnelse" description="Oppdater påminnelsen." form={form} onChange={update} onSubmit={()=>mutation.update(form)} onCancel={()=>router.back()} submitting={mutation.saving} error={mutation.error} familyMembers={members.familyMembers} submitTitle="Lagre endringer"/></View>};const styles=StyleSheet.create({root:{flex:1,backgroundColor:theme.colors.background},top:{padding:theme.spacing.lg,flexDirection:"row",justifyContent:"space-between",alignItems:"center"}});
