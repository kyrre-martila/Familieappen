import { Modal, Pressable, StyleSheet, View } from "react-native";
import { AppText, Button, Card } from "../../components";
import { theme } from "../../theme/tokens";
const actions = ["Kalenderhendelse", "Oppgave", "Handlelistevare", "Middag", "Ønskeliste"];
export function CreateActionSheet({ visible, onClose }: { visible: boolean; onClose: () => void }) { return <Modal animationType="slide" transparent visible={visible} onRequestClose={onClose}><Pressable accessibilityRole="button" accessibilityLabel="Lukk opprett meny" style={styles.backdrop} onPress={onClose}><Pressable style={styles.sheet}><Card><AppText variant="label">Opprett</AppText><AppText variant="heading">Hva vil du legge til?</AppText><AppText style={{ color: theme.colors.textMuted }}>Dette er et midlertidig Run 0-panel. Valgene blir funksjonelle senere.</AppText><View style={styles.actions}>{actions.map((action) => <Button key={action} title={action} variant="secondary" accessibilityLabel={`${action} kommer senere`} disabled />)}</View><Button title="Lukk" variant="ghost" onPress={onClose} /></Card></Pressable></Pressable></Modal>; }
const styles = StyleSheet.create({ backdrop: { flex: 1, justifyContent: "flex-end", backgroundColor: "rgba(34,34,34,0.24)" }, sheet: { padding: theme.spacing.lg, paddingBottom: theme.spacing.xl }, actions: { gap: theme.spacing.sm } });
