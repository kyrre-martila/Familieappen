import { PlaceholderPage } from "../../components/PlaceholderPage";

export default function TasksPage() {
  return (
    <PlaceholderPage
      description="Track shared chores, reminders and practical jobs without adding unnecessary complexity."
      label="Tasks"
      title="Tasks"
    />
  );
}
