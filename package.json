{
  "name": "familieappen",
  "version": "0.1.0",
  "private": true,
  "description": "Lean fullstack monorepo foundation for FamilieAppen.",
  "packageManager": "pnpm@10.12.1",
  "scripts": {
    "dev:web": "pnpm --filter @familieappen/web dev",
    "dev:mobile": "pnpm --filter @familieappen/mobile start",
    "dev:api": "pnpm --filter @familieappen/api start:dev",
    "start:web": "pnpm --filter @familieappen/web start",
    "start:api": "pnpm --filter @familieappen/api start",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "prisma:generate": "pnpm --filter @familieappen/api prisma:generate",
    "prisma:migrate": "pnpm --filter @familieappen/api prisma:migrate",
    "prisma:studio": "pnpm --filter @familieappen/api prisma:studio"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=10.0.0"
  }
}
